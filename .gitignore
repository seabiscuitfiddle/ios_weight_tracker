# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode — the project is generated from project.yml by XcodeGen, never committed.
# Run `xcodegen generate` after cloning. See README.
Tally.xcodeproj/
*.xcworkspace/
DerivedData/
build/
*.xcuserstate
*.xcscmblueprint

# macOS
.DS_Store

# Secrets — the Anthropic API key is entered in-app and stored in the Keychain.
# Nothing here should ever hold one.
*.p8
*.p12
*.mobileprovision
Secrets.xcconfig
